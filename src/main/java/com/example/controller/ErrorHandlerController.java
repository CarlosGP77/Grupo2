package com.example.controller;

/**
 * Spring Boot ya expone su propio `BasicErrorController` en `/error`.
 *
 * Esta clase se deja sin anotaciones para evitar el conflicto de mapeo
 * que impedía arrancar la aplicación.
 */
public class ErrorHandlerController {
}

