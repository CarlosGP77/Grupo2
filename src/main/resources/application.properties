spring.application.name=BdConSprintBoot
# Use environment variables when available. Defaults kept for local development.
spring.datasource.url=${DB_URL:jdbc:mariadb://192.168.35.132:3306/mourosub}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:admin_123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true